<!DOCTYPE html>
<html>
  <head>
    <title>Rocky Jaiswal</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf-8" />
    <meta http-equiv='X-UA-Compatible' content='IE=edge;chrome=1' />
    <%= stylesheet_link_tag 'app' %>
    <%= stylesheet_link_tag 'bootstrap' %>
    <%= stylesheet_link_tag 'bootstrap-responsive' %>
    <%= javascript_include_tag 'bootstrap' %>
    <link href='http://fonts.googleapis.com/css?family=Ultra|Play' rel='stylesheet' type='text/css'>
  </head>

  <body>

    <div class="banner">
        <h1 class="bottom-padded-25">
          <a class="dark-chocolate" href="/">Still Learning ...</a>
        </h1>
        <p class="play">Musings of an agile developer</p>
    </div>

    <div id="main" role="main" class="container-fluid">

      <div class="row-fluid">
        <div class="span9">
          <% if is_blog_article? %>
            <% content_for :blog_article, yield %>
            <%= partial settings.blog_article_template %>
          <% else %>
            <%= yield %>
          <% end %>
        </div>

        <div class="span3">
          <h2>
            <img src="/images/Rocky_Jaiswal.jpg" alt="Rocky Jaiswal" style="max-width: 100%;"/>
            <br/>
            <a href="/about">About</a>
          </h2>
          <hr/>
          <h2>
            <a href="/archives">Recent Articles</a>
          </h2>
          <ol>
            <% data.blog.articles[0...20].each do |article| %>
              <li>
                <a href="<%= article.url %>"><%= article.title %></a> 
                <span><%= article.date.strftime('%b %Y') %></span>
              </li>
            <% end %>
          </ol>
          <hr/>
          <h2>
            <a href="/tags">Tags</a>
          </h2>
          <ul>
            <% data.blog.tags.first(9).each do |slug, tag| %>
              <li>
                <h5><%= tag.title %></h5>
                <ul>
                  <% tag.pages.each do |title, url| %>
                    <li>
                      <a href="<%= url %>"><%= title %></a>
                    </li>
                  <% end %>
                </ul>
              </li>
            <% end %>
          </ul>
        </div>
      
      </div>
    </div>
    
    
  </body>
</html>
